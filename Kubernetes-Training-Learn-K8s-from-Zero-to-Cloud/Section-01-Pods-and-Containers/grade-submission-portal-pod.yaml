#-----------------------------------------------------------------------
# File: grade-submission-portal-pod.yaml
#
# Author: Emmanuel Taylor
# Created: June 29, 2025
# Modified: June 30, 2025
#
# Description:
# Kubernetes Pod definition for the grade-submission-portal application.
# 
# This Pod runs two containers:
# - The main frontend service (grade-submission-portal)
# - A sidecar health checker container (grade-submission-portal-health-checker)
#
# The Pod is labeled for frontend identification and can be targeted by
# services or policies. Resources are constrained for controlled CPU and
# memory usage.
#-----------------------------------------------------------------------

apiVersion: v1
kind: Pod
metadata:
  name: grade-submission-portal
  labels:
    app.kubernetes.io/name: grade-submission
    app.kubernetes.io/component: frontend
    app.kubernetes.io/instance: grade-submission-portal
spec:
  containers:
  - name: grade-submission-portal
    image: rslim087/kubernetes-course-grade-submission-portal
    resources:
      requests:
        memory: "128Mi"
        cpu: "200m"
      limits:
        memory: "128Mi"
    ports:
      - containerPort: 5001
  - name: grade-submission-portal-health-checker
    image: rslim087/kubernetes-course-grade-submission-portal-health-checker
    resources:
      requests:
        memory: "128Mi"
        cpu: "200m"
      limits:
        memory: "128Mi"