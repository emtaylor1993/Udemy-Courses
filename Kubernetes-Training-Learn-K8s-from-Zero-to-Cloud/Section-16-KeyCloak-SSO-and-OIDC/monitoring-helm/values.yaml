# ------------------------------------------------------------------------------
# File: values.yaml
#
# Author: Emmanuel Taylor
# Created: July 29, 2025
# Modified: July 29, 2025
#
# Description:
# Helm values configuration for customizing the Grafana component of the 
# kube-prometheus-stack Helm chart.
#
# This file:
# - Configures Grafana to use Keycloak for OAuth2 login via OIDC.
# - Enables user signup and sets up generic OAuth integration.
# - Customizes how Grafana is accessed externally through root_url.
# - Disables deployment of node-exporter due to incompatibility with Docker Desktop.
# ------------------------------------------------------------------------------

grafana:
  grafana.ini:
    server:
      # Define how Grafana is accessed externally
      # You may need to adjust this based on your ingress or NodePort setup
      root_url: "http://localhost:3000/"
    auth:
      disable_login_form: false
    auth.generic_oauth:
      verbose_logging: true
      enabled: true
      name: Keycloak
      allow_sign_up: true
      client_id: grafana
      client_secret: 30neh2Vp3LL8KaF5BJ9cD6bb3spO2nz7
      scopes: openid email profile
      # auth_url must be accessible from the user's browser
      auth_url: http://localhost:30080/realms/grafana/protocol/openid-connect/auth
      # token_url and api_url are server-to-server communications
      token_url: http://keycloak.keycloak.svc.cluster.local:80/realms/grafana/protocol/openid-connect/token
      api_url: http://keycloak.keycloak.svc.cluster.local:80/realms/grafana/protocol/openid-connect/userinfo
      role_attribute_path: "contains(realm_access.roles[*], 'admin') && 'Admin' || 'Viewer'"      
      login_attribute_path: preferred_username
      name_attribute_path: name
      email_attribute_path: email
nodeExporter:
  enabled: false